adi: contains input and output for adipose tissue

gas: contains input and output for gastro tissue

islet: contains input and output for islet tissue

kid: contains input and output for kidney tissue

liver: contains input and output for liver tissue

*.txt: input files generated by input_file_generator.py 

results_mult.txt: lists some of the CV results for individual tissue training (example output script)

input_file_generator.py: generates the input files for training, as well as processes the output files 
(depending on the mode) for a given set of hyperparameters.
