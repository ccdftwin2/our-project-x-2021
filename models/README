######################################################
Readme for the models folder
#####################################################

flex_nn_model.py: final "model funciton": generates model based on given number of layers, layer sizes,
dropout rates, activations funcitons, and l2 parameter per layers.


*.py: Old models, not used for submitted results.
